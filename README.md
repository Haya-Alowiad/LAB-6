# LAB-6
Name: Haya Adnan Alowiad
ID: 2240007408
Section: 6FS4

===============================================

The dataset was loaded into a DataFrame using pandas and explored using functions such as head(), info(), and describe() to understand its structure and statistical properties.

No missing values were found in the dataset, so no data cleaning was required.

Linear Regression was used to build the model after selecting the relevant numerical features and excluding unnecessary columns such as Email and Address.

The model was trained using training data and evaluated using test data. Predictions were visualized using a scatter plot, which showed a strong linear relationship between actual and predicted values.

The evaluation metrics (MAE, MSE, RMSE) indicate that the model performs well.

The most important feature affecting yearly customer spending is Length of Membership, as it has the highest impact on the model.
